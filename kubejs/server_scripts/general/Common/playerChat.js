/*ServerEvents.tick(event =>{
let block = event.block
let level = block.level
let pos = block.pos
let blockState = block.blockState
event.server.tell(blockState.getDestroySpeed(level.getChunkAt(pos),pos))})*/